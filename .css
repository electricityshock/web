/* CSS for styling the chat interface */
.chat-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: #fff;
}

.chat-window {
  width: 100%;
  height: calc(100% - 100px); /* Adjust according to the height of user input and delete chat button */
  overflow-y: auto;
  padding: 20px;
}

.user-input {
  position: absolute;
  bottom: 60px; /* Adjust based on the height of delete chat button */
  left: 20px;
}

.user-input input {
  width: calc(100% - 80px); /* Adjust based on the width of the speech button */
  padding: 10px;
  margin-right: 10px;
}

.delete-chat {
  position: absolute;
  bottom: 10px;
  right: 10px;
}

.delete-chat button {
  background-color: #ff0000;
  color: #fff;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
}

.delete-chat button:hover {
  background-color: #cc0000;
}
